export interface Quiz {
  id: string;
  title: string;
  createdAt: string;
  updatedAt: string;
  file: string;
  groupId: string;
  startDate: string;
  duration: number;
  teacherId: string;
  questionCount: number;
  status: string;
} 